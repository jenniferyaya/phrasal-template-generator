var colour = 'A colour (orange, indigo, etc.)';

var options = prompt(colour);
document.write(options);


var text = 'After snacking on {adjective} treats, the {colour} bellied {creature} {verbed} for hours.';
var adjective = '';
